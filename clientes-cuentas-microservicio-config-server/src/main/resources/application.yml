server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/javierGomez18/clientes-cuentas-microservicio
          default-label: develop
          search-paths: config-server
          clone-on-start: true