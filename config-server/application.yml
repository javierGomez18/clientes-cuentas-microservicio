management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,refresh,mappings
  endpoint:
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}